# Example Docker Compose Template

# This file serves as a template for adding Docker Compose configurations
# Remove this comment block when adding actual configuration

# Example docker-compose.yml:
# version: '3.8'
# services:
#   web:
#     build: .
#     ports:
#       - "3000:3000"
#     environment:
#       - NODE_ENV=production
#     depends_on:
#       - db
#   
#   db:
#     image: postgres:15-alpine
#     environment:
#       POSTGRES_USER: user
#       POSTGRES_PASSWORD: password
#       POSTGRES_DB: mydb
#     volumes:
#       - postgres_data:/var/lib/postgresql/data
#
# volumes:
#   postgres_data:
